// Auto-generated by scripts/generate-mcp-wrappers.ts. Do not edit.
import type { Client } from "@modelcontextprotocol/sdk/client/index.js";
import type { CallToolRequest, CallToolResult, Tool } from "@modelcontextprotocol/sdk/types.js";

export const getMessagesTool: Tool = {
  name: "get_messages",
  description: "Get recent messages by chat_id or participant handle (phone/email).",
  inputSchema: {
    type: "object",
    properties: {
      chat_id: {
        type: "integer",
        description: "Numeric chat identifier from list_chats."
      },
      participant: {
        type: "string",
        description: "Phone number or email handle to scope the query."
      },
      limit: {
        type: "integer",
        minimum: 1,
        maximum: 500,
        default: 50,
        description: "Maximum number of messages to return (defaults to 50)."
      },
      context_anchor_rowid: {
        type: "integer",
        description: "Anchor message_rowid for contextual expansion."
      },
      context_before: {
        type: "integer",
        minimum: 0,
        maximum: 200,
        default: 10,
        description: "Messages to include before the anchor (0-200)."
      },
      context_after: {
        type: "integer",
        minimum: 0,
        maximum: 200,
        default: 10,
        description: "Messages to include after the anchor (0-200)."
      },
      context_include_attachments_meta: {
        type: "boolean",
        description: "Include attachment metadata in contextual results."
      }
    },
    additionalProperties: false,
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
} as const;

export type GetMessagesArgs = CallToolRequest["params"]["arguments"];

export async function callGetMessages(client: Client, args: GetMessagesArgs = {}): Promise<CallToolResult> {
  return client.callTool({
    name: "get_messages",
    arguments: args ?? {},
  });
}
